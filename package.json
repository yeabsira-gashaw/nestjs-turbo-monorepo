{
  "name": "api",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "dependencies": {
    "@wavesync/postgres": "workspace:*",
    "@wavesync/config": "workspace:*",
    "@wavesync/mongo": "workspace:*"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.1",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
